using System;

public class Solution {
    public int solution(string message) {
        int answer=0;
        int sizeMessage= message.Length*2;
        answer = sizeMessage;
        return answer;
    }
}